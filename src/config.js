// config.js
const config = {
  API_ENDPOINT: 'https://dev.api.goongoonalo.com/v1',
  token: '', // Initially empty, set during runtime
  requestId: '', // Initially empty, set during runtime
  otp: '5678',
  userId: '+919000000000', // Update this with the actual user ID
};

export default config;
